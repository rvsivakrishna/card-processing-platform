apiVersion: v1
kind: Service

metadata:
  name: card-processor-service
  namespace: card-processing

spec:

  selector:
    app: card-processor

  ports:

  - port: 8080
    targetPort: 8080

  type: ClusterIP
